// ----------------------------------------------------------------------
// Shared measurements for the desktop nav tabs: the bar, the tabs inside it
// and the pill that slides between them all read from here so they line up.

export const TAB_HEIGHT = 32;

/** Space between the pill and the edge of the bar. */
export const TAB_BAR_PADDING = 4;

export const TAB_DURATION = '250ms';
export const TAB_EASING = 'cubic-bezier(0.22, 1, 0.36, 1)';
